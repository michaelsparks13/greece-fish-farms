# MapTiler Cloud key for basemap tiles (https://cloud.maptiler.com/account/keys/).
# Copy to .env.local for local dev. Deploys read it from the Netlify site's environment
# variables (and the GitHub Actions secret). Without it the site falls back to CARTO tiles.
NEXT_PUBLIC_MAPTILER_KEY=your-maptiler-key
